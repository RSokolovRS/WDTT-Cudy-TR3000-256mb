#!/bin/sh
# Restart WDTT when WAN or WWAN comes up

[ "$ACTION" = "ifup" ] || exit 0
case "$INTERFACE" in
wan|wwan) ;;
*) exit 0 ;;
esac

enabled="$(uci -q get wdtt.globals.enabled 2>/dev/null)"
[ "$enabled" = "1" ] || exit 0

/etc/init.d/wdtt restart

exit 0
